import 'package:flutter/material.dart';

/// Colors matching the app's dark/teal login screen design.
class AppColors {
  AppColors._();

  static const background = Color(0xFF11151B);
  static const surface = Color(0xFF171C24);
  static const fieldBorder = Color(0xFF2A313D);
  static const accent = Color(0xFF4FE0C6);
  static const accentDim = Color(0xFF2E7D6E);
  static const textPrimary = Color(0xFFEAEDF2);
  static const textSecondary = Color(0xFF8A93A3);
  static const error = Color(0xFFFF6B6B);
}
